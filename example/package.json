{
  "author": {
    "name": "Carl Broadbent"
  },
  "dependencies": {
    "event-iterable": "file:..",
    "node-abort-controller": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.5"
  },
  "engines": {
    "nodejs": "12.18.3"
  },
  "files": [
    "config/*",
    "js/*",
    "types/*"
  ],
  "license": "UNLICENSED",
  "scripts": {
    "build": "( cd .. && npm run build ) && tsc",
    "clean": "rimraf js node_modules package-lock.json types",
    "prepare": "npm run build",
    "rebuild": "npm run unbuild && npm run build",
    "unbuild": "( cd .. && npm run unbuild ) && rimraf js types"
  }
}
